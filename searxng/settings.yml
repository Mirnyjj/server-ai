use_default_settings: true

general:
  debug: false
  instance_name: "IG Agent Search"

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: "${SEARXNG_SECRET}"
  bind_address: "0.0.0.0"
  limiter: false
